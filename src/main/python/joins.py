class JoinInfo:
  def __init__(self, metadata, jobprops):
    self.meta = metadata
    self.jobprops = jobprops
    jointabs = {}
    # for k, v in props.iteritems():
    #   jointabs[k] = v if k
jointabs = ['customer','product','billing']
